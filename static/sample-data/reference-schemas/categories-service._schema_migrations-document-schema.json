{
    "_id": {
        "$oid": "string (UUID/Hex)"
    },
    "version": "string",
    "description": "string",
    "checksum": "string (hex-encoded SHA-256)",
    "appliedAt": {
        "$date": "string (ISO-8601 UTC)"
    }
}